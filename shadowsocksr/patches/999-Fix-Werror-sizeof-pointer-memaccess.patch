--- a/src/local.c
+++ b/src/local.c
@@ -721,7 +721,7 @@
 
                     ss_free(hostname);
                 } else {
-                    strncpy(host, ip, sizeof(ip));
+                    strncpy(host, ip, INET6_ADDRSTRLEN);
                 }
             }
 
